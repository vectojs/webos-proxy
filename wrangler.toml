name = "webos-proxy"
main = "src/index.ts"
compatibility_date = "2026-08-01"

# The custom domain `proxy.vectojs.org` is attached to this worker out-of-band
# (Workers Custom Domains API — the token lacks Zone Workers Routes scope).
# See README "Deploy" for the one-time domain attachment.

[observability]
enabled = true
